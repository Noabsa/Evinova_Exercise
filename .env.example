# Copy to .env and fill in. .env is git-ignored; this file is not.

# With a key, submissions are extracted by Claude. Without one, the service
# falls back to the deterministic fake extractor and says so at startup.
ANTHROPIC_API_KEY=
